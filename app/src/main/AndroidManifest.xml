<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.e.booker">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Booker"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".view.ui.activities.home.ProfileChangeActivity"></activity>
        <activity android:name=".view.ui.activities.home.ProfileSettingsActivity" />
        <activity android:name=".view.ui.activities.TestActivity" />
        <activity android:name=".view.ui.activities.adminpanel.AdminPanelActivity" />
        <activity android:name=".view.ui.activities.home.HomeActivity" />
        <activity android:name=".view.ui.activities.auth.LoginAcitivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.ui.activities.auth.RegistrationActivity" />
    </application>

</manifest>