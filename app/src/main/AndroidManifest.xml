<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.e.booker">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Booker"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">
        <activity android:name=".view.ui.activities.TestActivity"></activity>
        <activity android:name=".view.ui.activities.AdminPanelActivity" />
        <activity android:name=".view.ui.activities.SearchBooksActivity" />
        <activity android:name=".view.ui.activities.HomeActivity" />
        <activity android:name=".view.ui.activities.LoginAcitivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".view.ui.activities.RegistrationActivity" />
    </application>

</manifest>